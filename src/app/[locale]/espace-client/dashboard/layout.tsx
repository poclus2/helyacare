// Ce layout est intentionnellement vide.
// La mise en page du dashboard ambassadeur est gérée par le layout parent
// de l'espace-client (/espace-client/layout.tsx).
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
