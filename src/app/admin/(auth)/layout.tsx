/**
 * Layout pour le groupe (auth) — /admin/login
 * Pas de sidebar, page standalone
 */
export default function AuthGroupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
